<nav class="navbar navbar-expand-lg bg-light navbar-light py-3 py-lg-0 px-0">
    <a href="" class="text-decoration-none d-block d-lg-none">
        <h1 class="m-0 display-5 font-weight-semi-bold"><span class="text-primary font-weight-bold border px-3 mr-1">E</span>Shopper</h1>
    </a>
    <button type="button" class="navbar-toggler" data-toggle="collapse" data-target="#navbarCollapse">
        <span class="navbar-toggler-icon"></span>
    </button>
    <div class="collapse navbar-collapse justify-content-between" id="navbarCollapse">
        <div class="navbar-nav mr-auto py-0">



            {% for link in linklists[section.settings.main_linklist].links %}

            {% if link.links == blank %}
            <a href="{{link.url}}" class="nav-item nav-link">{{link.title}}</a>
            {% endif %}
            
            {% endfor %}



        </div>
        <div class="navbar-nav ml-auto py-0">
            {%if shop.customer_accounts_enabled%}
            {% if customer%}
            <a href="/account" class="nav-item nav-link">Your Account</a>
           
            <a href="/account/login" class="nav-item nav-link">Sign Out</a>
            {%else%}
            <a href="/account/login" class="nav-item nav-link">Login</a>
            <a href="/account/register" class="nav-item nav-link">Signup</a>
            {%endif%}
            {%endif%}
        </div>
    </div>
</nav>